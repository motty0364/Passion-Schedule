<h1>Public::Projects#show</h1>
<p>Find me in app/views/public/projects/show.html.erb</p>

<%= @project.inspect %>

<%= link_to "edit", edit_project_path(@project) %>

<%= link_to "destroy", project_path(@project), method: :delete, data: { confirm: "削除しますか？" } %>
